# Local Claude settings (per-machine, not shared)
.claude/settings.local.json

# Node / Vite (aún sin código, preparado para v1)
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo

# OS
Thumbs.db
.DS_Store

# Mirror de imagenes de carta (SPEC-041): ~226 MB, van al repo del mirror, no aqui
card-images/
.claude/settings.json
